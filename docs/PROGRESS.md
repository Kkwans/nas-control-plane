# NAS Control Plane 阶段进度

更新时间：2026-07-19

## 当前结论

项目仍处于 Spec 定义的 **Phase 0：技术验证**。前后端工程、统一设计系统和五项受控能力 PoC 已形成可验证基础；尚未进入完整管理面板、认证、数据库迁移或生产运行阶段。

## 已完成

| 范围 | 状态 | 证据与边界 |
| --- | --- | --- |
| P0-01 环境能力探测 | 已完成 | Go 单测、ARM64 交叉构建和 NAS 实机探测通过。 |
| P0-02 Docker SDK 探针 | 已完成 | 仅针对固定标签的临时容器完成实机验证，验证容器已清理。 |
| P0-03 Agent Unix Socket 与 HTTP 适配层 | 代码完成 | 代码、测试和 ARM64 构建已完成；因当前没有非交互 root 提权条件，systemd 常驻 Agent 未做实机验证。 |
| P0-04 受控终端 PoC | 已完成 | PTY、WebSocket、尺寸同步、退出和资源上限均经 NAS 实机 PoC 验证；仅通过显式参数开启。 |
| P0-05 journald PoC | 已完成 | 查询、筛选、Cursor 分页和 Follow 流均经 NAS 实机 PoC 验证。 |
| P0-06 Caddy 路由持久化 PoC | 代码完成，运行时暂停 | 受限 Admin API 客户端和持久化编排已测试并提交；NAS 当前无运行中的 Caddy，运行时验证留待后续阶段。 |
| Vue 控制台 | 已完成基础界面 | 已建立总览、服务中心、能力地图、响应式布局、统一间距/圆角/色彩/动效语言；数据仍以明确标识的预览采样为主。 |

## 本次阶段收尾

本次只完成静态前端预览的 Compose 部署，不启动 Agent、不挂载 Docker Socket、不授予容器特权，也不改动其他 NAS 项目。

- Compose 项目名：`nas-control-plane`
- 容器名：`nas-control-plane-console`
- 计划端口：`8760`
- 项目登记：使用标准 Docker Compose 项目元数据，由 `docker compose ls` 识别；不写入绿联未公开的内部数据库。
- API 行为：`/api/` 明确返回 Phase 0 尚未部署的状态，前端进入预览态，不伪装为真实系统数据。

部署完成后的访问地址、健康检查和实际项目登记结果将在本文件下一次更新中记录。

## 尚未开始或未完成

- 用户认证、权限模型、审计事件与 Job 系统。
- SQLite/PostgreSQL 数据模型、可重复迁移与备份恢复能力。
- 完整 Docker/Compose 管理、日志中心、文件管理、告警和实时指标。
- 常驻特权 Agent 的 systemd 安装与 Unix Socket 运行时验证。
- Caddy 运行时路由持久化 PoC。
- 前端真实 API 接入、端到端测试和生产级发布链路。

## 下次继续顺序

1. 在明确的运行时授权和回滚方案下完成 P0-06 Caddy 实机 PoC，或根据实际环境调整验证对象。
2. 收敛 Phase 0 结论并决定是否进入 Phase 1 的认证、数据模型和 Job 基础设施。
3. 按 API 契约逐页接入真实只读数据，保持现有 UI 的状态语言和视觉规范。
