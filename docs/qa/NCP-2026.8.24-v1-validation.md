# NCP 2026.8.24-v1 验证记录

日期：2026-08-24
环境：`DH4300Plus` / `aarch64` / `/volume2/Project/nas-control-plane`

本记录对应本轮产品化收敛的已验证范围。NCP 仍按可信内网、登录后的 Root 控制面交付；本轮没有新增 MFA、RBAC、限流、复杂密码策略、CSP 或其他安全加固边界。

## 已交付

- 修复 system store 按资源维度的刷新并发，避免不同 scope 共用一个 Promise 时丢请求。
- 修复数据库表格中 `NULL`、空字符串和 bigint/decimal 文本值的展示与编辑语义。
- 统一 Monitoring 的共享 DTO，移除前端对内部字段副本的依赖。
- 使用 kin-openapi 做 OpenAPI 完整解析校验，并以 Go AST 检查注册路由与规格路径的 parity。
- 统一 `VERSION`、Compose `BUILD_VERSION`、镜像 tag、前端 package version 和 Go `--version` 为 `2026.8.24-v1`。
- 增加 GitHub Actions 核心门禁、Mock API Playwright Chromium 回归、Axe serious/critical 基线和 Compose config gate。
- 将 pnpm 初始化、Root Agent PTY 测试环境、跨 Chromium screenshot 容差和版本 tag 发布 job 收敛到可执行的 GitHub Actions；main push 的完整 CI 已有远端成功记录。
- 将 Infrastructure 首屏的网络、DNS、Tailscale、Mihomo 状态收敛为可读摘要；移动端系统分类改为可操作的 2×2 布局。
- 将 InfrastructureView 按职责拆分为 Overview、Storage、Services、Network、DNS、Proxy 和 Listeners 子面板；父组件继续持有请求、刷新、DNS 写入和 Mihomo 检查状态机，避免行为重写。
- 为 Network、Proxy、Listeners 的真实 Chromium normal 状态补充 1440×900、1280×800、768×1024、390×844 截图基线；滚动后检查代理链路和监听服务，不再只验证系统首屏。
- 为系统详情补充 loading skeleton、DNS 编辑 Dialog 取消、详情失败重试三类 Mock Chromium 状态回归。
- 将导航元数据抽到 `frontend/src/router/navigation.ts`，修复 Element Plus `v-loading` 未注册造成的运行时警告。
- 将 App、Infrastructure 和 Monitoring 的手动刷新从隐式 `window CustomEvent` 改为按生命周期注册的 typed refresh registry；顶部刷新会等待页面处理完成，并补充失败隔离回归测试。
- 将 `DockerImagePanel.vue` 中 Docker Hub 搜索、仓库详情、标签选择和拉取队列拆到 `DockerHubBrowser.vue`；父组件保留本地镜像和下载任务编排。
- 将 Docker 镜像、Docker Hub 和 Job API 从 `api/system.ts` 机械拆到 `api/docker.ts`；保留 system facade，Compose deploy 继续复用统一 Job 解析。
- Docker Mock API 增加本地镜像、Hub 搜索、标签、拉取任务和失败状态；Docker E2E 覆盖项目危险操作、Hub normal/empty/error、Axe 及四档 viewport screenshot regression。
- 为 Infrastructure 系统详情与 Mihomo 检查增加独立 request sequence gate；较早响应、错误和 finally 不再覆盖较新的刷新结果，并补充单元测试与延迟响应 E2E。
- 将 Infrastructure 的接口主次归类、监听端口聚合、范围筛选和分页状态抽到 `useInfrastructureNetwork`；保留原模板/CSS，并补充 composable 单测。
- 将 `api/system.ts` 的共享请求传输层抽到 `api/systemTransport.ts`；保留 `@/api/system` facade 和领域函数签名，后续仍需按领域继续拆分。
- Logs 历史查询接入服务端 `nextCursor`，并在界面明确单次最多 200 条；Docker 容器 bounded tail 没有历史游标时单独提示限制，补充 cursor E2E。
- 为 Database SQL 工作台增加 DROP/TRUNCATE/ALTER 及无 WHERE UPDATE/DELETE 的客户端风险提示与确认；取消确认不会发起 SQL 请求，提示不限制 Root 正常执行能力。
- 将 DatabaseView、DatabaseDetailView、DatabaseTableView 的发现失败状态收敛到共享 `DatabaseErrorPanel.vue`，保留原错误码、恢复动作和 Root 能力边界，并补充 Chromium 错误态回归。
- 固定前端直接依赖版本，移除 `package.json` 与 lockfile importer 中的 `latest`，避免重新解析时跨 major 漂移。
- 为 `/assets/` 增加 hashed 静态资源一年 immutable 缓存，为 `/index.html` 保留 no-cache；server/console 增加 `json-file` `max-size=10m`、`max-file=3` 日志轮转。
- 保留 Overview/System 的四档 viewport before/after 截图，并覆盖主要页面 desktop/mobile 正常导航与横向溢出检查。

## Finding 状态

| Finding | 状态 | 证据或说明 |
| --- | --- | --- |
| COR-01 systemStore 多 scope 刷新竞态 | 已解决 | `e265b9d`，并有 store 回归测试 |
| COR-02 数据库 NULL/空字符串/精度 | 已解决 | `e265b9d`，并有 value conversion 回归测试 |
| API-01 OpenAPI 校验过浅、route parity 缺失 | 已解决 | `5ddb0e4`，kin-openapi + AST parity |
| API-03 Monitoring DTO 漂移 | 已解决 | `5ddb0e4`，统一 `MetricSample`/`SystemSummary` |
| REL-01 VERSION/image/release 标识漂移 | 已解决，tag 发布待首次触发 | `02ff4ba`、`e7c0037`、`6c3aaec`；仓库、Compose、镜像和二进制均为 v1，Release title 固定为 `NCP <VERSION>`，当前未主动创建 release tag |
| QA-01 GitHub CI 缺失 | 已补齐并远端通过 | `73d5853`、`fb99c56`、`4ccb47e`、`374b6d3`；Actions run `32679683909` 的 Go/Frontend/Compose jobs 全部 success |
| QA-02 Playwright 只有配置 | 已解决 | `73d5853`、`1d4120c`、`5728b65`、`374b6d3` |
| FE-06 导航元数据重复 | 已解决 | `f9adacc` |
| FE-04 页面级隐式手动刷新 | 已解决 | `c4c3b82`；App 提供 typed registry，页面按挂载生命周期注册/注销，注册表单测覆盖等待、注销和失败隔离 |
| FE-02 Infrastructure 首屏信息堆叠 | 本轮已收敛 | `92fd3cd`、`97bb1ff`、`d579004`、`10f341c`、`a286377`、`9db173d`、`230b32f`、`c79ab52`、`23c4e5a`；摘要与各能力面板已按职责拆分，状态机仍由父组件编排，残留 CSS 清理归入 FE-05 |
| FE-01 `api/system.ts` 大模块 | 主要职责已拆分 | `739d493`、`a256f87`、`5ab5bb5`、`dbf361c`、`9ef8b4e`；transport、认证、系统信息、Docker 镜像/Hub/Job/容器/Compose/日志客户端均已抽出，facade 仍集中导出 DTO 类型以保持兼容 |
| FE-03 `DockerImagePanel.vue` 大模块 | 本轮机械拆分 | `46a087b`；Hub 子职责已移出，下载任务和本地镜像仍由父组件编排 |
| FE-05 重复/覆盖 CSS 与 lint/format 门禁 | 仍存在，延期 | 本轮没有大范围 CSS 清理，也没有新增 ESLint/Prettier gate；避免把结构拆分与视觉重写混在一个变更中 |
| STATE-02 Infrastructure stale response | 已解决 | `922407a`；独立 request sequence gate、单元测试和延迟响应 E2E |
| UX-01～UX-02 Docker 项目入口与生命周期确认 | 已收敛 | `abaa27a`、`46a087b`；start 直达，stop/restart/危险操作按影响确认并有 Chromium 回归 |
| UX-03 高风险动作统一确认 | 部分完成 | Docker 生命周期和 SQL 风险已覆盖；Users 及跨页面统一 `ConfirmDangerDialog`/恢复信息仍需后续收敛 |
| UX-04 HostPathPicker / NAS 路径浏览 | 仍存在，延期 | 当前没有可复用的 BrowsePath/ListDirectory Agent RPC；本轮没有用限制目录的伪选择器替代 Root 的 NAS 路径能力 |
| UX-05 Network/Volume selector | 仍存在，延期 | 创建容器表单的现有资源选择与高级创建语义尚未独立收敛 |
| UX-06 数据库类型编辑器 | 正确性已解决，交互仍部分完成 | `e265b9d` 已覆盖 NULL/空字符串及 bigint/decimal 文本精度；按列类型 CellEditor 仍未完整落地 |
| UX-08 Logs 固定 limit/游标语义 | 部分完成 | `abccab6`、`56cf1c8`；system/agent 接入 `nextCursor`，容器日志仍为 bounded tail 并明确提示 |
| UX-07 SQL 高影响操作提示 | 已解决 | `99d0091`；风险分类单测与取消确认 Chromium E2E，Root 仍可正常执行 |
| UX-09 Monitoring 开发字段文案 | 当前实现更合理 | 当前主文案使用“暂无磁盘 I/O/温度历史样本”等用户语义，字段名仅保留在代码/诊断数据中 |
| UX-10 信息架构与导航术语 | 部分完成 | `f9adacc` 已统一导航元数据；资源/可观测/主机/管理分组及全部术语迁移仍未完成 |
| UX-11 跨页面错误状态模型 | 部分完成 | `1d54eac` 先收敛三张 Database 页面共享错误面板；全站 InlineError/ResourceState/error catalog 仍未完成 |
| UX-12 列表/toolbar/empty/pagination primitive | 部分完成 | 已有 WorkspaceHeader、ActionButton、NcpSelect、SegmentedTabs 等基础并在局部复用；全站迁移和状态一致性仍延期 |
| A11Y-01 语义控件与 axe | 部分完成 | 关键页面已有 Chromium + axe serious/critical 基线；全主要页面语义控件审计仍未完成 |
| A11Y-02 route/query focus | 未专项验收 | 当前没有专门针对 drawer/filter query 变化的焦点回归证据 |
| DESIGN-01 图表/终端硬编码 token | 仍存在，延期 | 本轮未建立 chart/terminal token adapter |
| DESIGN-02 shared primitive 迁移 | 部分完成 | 保留并强化既有 primitive；业务页 bespoke 变体尚未全部收敛 |
| DESIGN-03 语义响应式断点 | 仍存在，延期 | 已有四档 viewport 回归，但各页面断点策略尚未统一为语义 token |
| DESIGN-04 密度组合视觉回归 | 未完成 | 尚未覆盖 13 compact / 15 comfortable / 18 comfortable 组合 |
| API-02 OpenAPI 生成前端 DTO | 仍存在，延期 | 已完成 OpenAPI full validation、route parity 和 Monitoring DTO 修复；前端仍保留手写 domain DTO facade |
| BACK-01～BACK-04 后端超大模块 | 仍存在，延期 | 本轮优先完成前端正确性、门禁和真实部署收敛，未对 handler/sites/container_create/manager 做跨模块拆分 |
| OBS-02 结构化 actor/action/duration 日志 | 仍存在，延期 | 未新增 slog 结构化日志迁移 |
| DEP-01 前端依赖 `latest` | 已解决 | `e1f39eb`；直接依赖与 lockfile importer 已固定版本 |
| DEP-02 基础镜像移动 tag | 仍存在，延期 | 本轮未改变 Dockerfile 基础镜像 pin 策略 |
| DEP-03 Compose 日志轮转 | 已解决并部署 | `b1e2ca3`；server/console 运行实例均核实 `json-file`、10m、3 文件 |
| WEB-01 静态资源缓存 | 已解决并部署 | `b1e2ca3`；运行实例核实 `/assets/*.js` 为一年 immutable、`/index.html` 为 no-cache |
| WEB-02 gzip/brotli | 仍存在，延期 | 本轮未引入压缩策略 |
| PERF-01～PERF-03 性能与 bundle budget | 仍存在，延期 | 构建警告已保留为证据，尚未建立 budget/visualizer 或后端 probe 优化 |
| TERM-01～TERM-02 终端正式命名与用户术语 | 仍需核对 | 本轮未改变现有终端协议/错误码边界，也未完成术语产品化审查 |
| DOC-01 README/架构/发布快照 | 未完成 | README、PROGRESS、设计文档当前存在用户未提交修改，本轮保留未覆盖；最终同步仍需在用户变更合并后完成 |
| QA-03 lint/format gate | 仍存在，延期 | 当前门禁已覆盖 Go/TS typecheck/unit/build/OpenAPI/Compose/E2E，但没有 lint/format script |
| QA-04 screenshot regression | 已补齐基础门禁 | `69af511`、`6476632`、`b151fd5`、`5728b65`、`fd9356e`、`374b6d3`、`dd1e290`；Overview、System 首屏、Network、Storage、Services、Proxy、Listeners 和 Docker Hub 已有四档固定 viewport 基线，跨页面状态全量截图仍未完成 |
| QA-05 axe baseline | 已补齐基础门禁 | `73d5853`；关键 E2E 做 serious/critical 检查，未宣称全页面零问题 |
| REFACTOR-01 Database 错误模型重复 | 首个切片已解决 | `1d54eac`、`6b5d089`；共享 `DatabaseErrorPanel` 已复用并修复详情页 `CircleAlert` 运行时注册，其他领域仍未统一 |
| REFACTOR-02 展示 formatter 重复 | 仍存在，延期 | 本轮未扩大为全局 presentation helper 重构 |
| 性能 chunk | 延期 | 构建仍有大 chunk 警告，未在没有测量预算的情况下贸然拆包 |

## 工程门禁实际结果

- `corepack pnpm run typecheck`：通过。
- `corepack pnpm test`：17 个 test files、81 个 tests 全部通过。
- `corepack pnpm build`：通过，4112 modules transformed；仍有既有大 chunk 警告，最大 `RealtimeTrendChart` 约 527.25 kB。
- Mock Chromium：固定 viewport 截图基线后，本地 `CI=1 corepack pnpm exec playwright test e2e/app.spec.ts e2e/docker.spec.ts e2e/system.spec.ts --workers=2 --timeout=120000 --reporter=line` 为 `5 passed (1.2m)`；`c90b63e` 前的本地完整 `CI=1 corepack pnpm test:e2e` 为 `13 passed (3.3m)`。GitHub Actions 最新 run `32684025341`（HEAD `c90b63e`）执行完整 `corepack pnpm exec playwright test`，报告 `Running 15 tests using 2 workers`、`15 passed (1.9m)`；包含主要页面四档 viewport、Docker 生命周期/Hub、SQL 风险提示、Database 发现失败、Logs cursor、Infrastructure stale response、loading/error/retry 和 DNS Dialog 状态。
- Infrastructure focused Chromium：`CI=1 corepack pnpm exec playwright test e2e/system.spec.ts --workers=1 --reporter=line`：`5 passed (52.1s)`；覆盖四档 viewport 的 Overview/Network/Storage/Services/Proxy/Listeners 截图、顶部刷新注册表链路、较早系统详情响应不能覆盖较新响应、加载 skeleton、DNS Dialog 取消和失败重试；Proxy/Listeners 截图基线位于 `frontend/e2e/system.spec.ts-snapshots/`。
- Docker focused Chromium：`corepack pnpm exec playwright test e2e/docker.spec.ts --workers=1 --timeout=120000 --reporter=line`：`2 passed (35.7s)`；覆盖 1440×900、1280×800、768×1024、390×844，Hub normal/empty/error、project dialog、Axe 和横向溢出检查。
- Logs focused Chromium：`corepack pnpm exec playwright test e2e/logs.spec.ts --workers=1 --timeout=120000 --reporter=line`：`1 passed (49.4s)`；验证服务端 cursor 传递、追加结果和无更多结果状态。
- Database focused Chromium：`corepack pnpm exec playwright test e2e/database.spec.ts --workers=1 --timeout=120000 --reporter=line`：`2 passed (11.2s)`；验证无范围 DELETE 的风险提示、取消确认、未发起 query 请求和数据库发现失败共享错误面板。
- GitHub Actions run `32679683909`：Go checks（`go test ./...`、`go vet ./...`、native/ARM64 build）success；Frontend checks（pnpm setup、typecheck、unit、build、Chromium E2E）success；Compose config success；main push 上 Publish ARM64 release 按 tag 条件 skipped，尚未执行实际 release tag 发布。
- 在功能基线 `b151fd5` 的干净 detached worktree 中，用户要求的完整 `go test ./...`、`go vet ./...` 和 `go build ./cmd/ncp-server ./cmd/ncp-agent` 均通过；本次后续提交仅涉及前端/测试/文档，没有改变 Go 源码。当前工作树直接运行完整 `go test ./...`/`go vet ./...` 仍会触碰 `data/backups/2026.7.25-v5` 并因 root-owned 权限拒绝退出 1，没有修改该回滚目录权限或内容。
- `./scripts/release/check-version.sh --server-binary bin/ncp-server-linux-arm64 --agent-binary bin/ncp-agent-linux-arm64`：通过，报告 `version consistency: 2026.8.24-v1`；server/agent 二进制 `--version` 均报告 v1。
- `git diff --check`：通过。
- `docker compose -f deploy/console/docker-compose.yml config`：通过，server image 为 `nas-control-plane:2026.8.24-v1`，console 继续只读挂载 `frontend/dist`，server/console 均声明 `json-file` 日志轮转。
- Dockerfile 在线构建曾因 `proxy.golang.org` EOF 失败；随后使用仓库已有 `Dockerfile.server.prebuilt` 和本地已验证 ARM64 二进制完成镜像构建，镜像为 `arm64/linux`。

## UI before/after 证据

Playwright Chromium 在当前 NAS 工作区、Vite Mock API 下执行；这是真实浏览器截图，不是源码推断。截图位于：

- [Overview before](./screenshots/2026.8.24-v1/before/overview-desktop-wide-chromium-linux.png) / [after](./screenshots/2026.8.24-v1/after/overview-desktop-wide-chromium-linux.png)
- [System before](./screenshots/2026.8.24-v1/before/system-desktop-wide-chromium-linux.png) / [after](./screenshots/2026.8.24-v1/after/system-desktop-wide-chromium-linux.png)
- [Docker Hub before](./screenshots/2026.8.24-v1/before/docker-hub-desktop-wide-chromium-linux.png) / [after](./screenshots/2026.8.24-v1/after/docker-hub-desktop-wide-chromium-linux.png)
- 同目录还保留 `1440×900`、`1280×800`、`768×1024`、`390×844` 四档图片；Infrastructure 的 Proxy/Listeners 真实浏览器基线另保留在 `frontend/e2e/system.spec.ts-snapshots/`，并在测试中滚动到对应面板后截图。

Docker before 使用 `abaa27a` 的拆分前代码和同一份 Mock API，after 使用 `46a087b`；真实截图显示移动端旧版仓库列表高度约束导致不可见，新版恢复列表并保留详情、搜索和操作区。当前正式截图基线覆盖 Overview/System/Docker Hub 的指定 normal 状态；主要页面覆盖 desktop/mobile 正常进入、标题、无横向溢出和浏览器 error。尚未把所有主要页面的 loading/empty/error/dialog/drawer 做成全量 screenshot regression，也没有宣称这些未覆盖状态已完成视觉验收。

## NAS smoke 与回滚

已在 `DH4300Plus/aarch64` 上完成最小范围部署验证：前端重新执行 `corepack pnpm build` 生成当前 HEAD 产物，执行 `docker compose -f deploy/console/docker-compose.yml up -d --no-deps server console`，只重建 NCP 的 server/console；没有重启 Agent，未修改数据库、数据卷或其他 Compose 服务。server 镜像 digest 未变，console 使用新 Nginx 配置，前端通过 `frontend/dist -> /usr/share/nginx/html` bind mount 提供。

- `nas-control-plane`：`nas-control-plane:2026.8.24-v1`，healthy。
- `nas-control-plane-console`：`nginx:alpine`，healthy。
- server 内 `/usr/local/bin/ncp-server --version`：`2026.8.24-v1`；server/console 均为本次部署后 healthy。
- 当前 `frontend/dist/index.html`：`2026-08-24 08:49:44 +0800`，SHA-256 为 `03ed6fe86fc6a7fa82f4199f8de66fb58d7c036d376e9c6dc0150839d7a1f861`。
- 容器内 `/healthz`：200。
- `http://127.0.0.1:8760/healthz`：200。
- `http://127.0.0.1:8760/`：200。
- `/api/v1/auth/status`：200。
- 未登录 `/api/v1/system/summary`、`/api/v1/docker/inventory`：401，符合认证边界。
- `docker exec nas-control-plane-console nginx -t`：成功；`/assets/index-BKxyPyWM.js` 返回 `Cache-Control: public, max-age=31536000, immutable`，`/index.html` 返回 `Cache-Control: no-cache`。
- `docker inspect`：server/console 均为 `json-file`，`max-size=10m`、`max-file=3`。

回滚点为仍保留的 `nas-control-plane:2026.8.11-v5`，镜像 ID 为 `sha256:615267acd61cba85e383c79f23d2cbfefb1081b303eca18f73779f1a579d7fb4`；当前新镜像 ID 为 `sha256:b4bc26b5fee7cd4421058bf5d8552c3ef930fd4227a47eac5eb1e900fd125ca9`。回滚应使用临时 Compose override 指向旧 tag，再执行 `docker compose up -d --no-deps server`，完成后重新检查 server health、`--version` 和 console API smoke；本记录没有执行回滚。

本次 `46a087b` 之后的 `05047f1`、`922407a`、`739d493`、`97bb1ff`、`abccab6`、`cb78cd0`、`56cf1c8`、`99d0091`、`a256f87`、`d579004`、`5ab5bb5`、`dbf361c`、`9ef8b4e`、`69af511`、`6476632`、`b151fd5`、`c4c3b82`、`429e8c0`、`ecc158b`、`1d54eac`、`6b5d089`、`e1f39eb`、`b1e2ca3`、`10f341c`、`a286377`、`9db173d`、`230b32f`、`c79ab52`、`23c4e5a`、`dd1e290`、`a4691b8`、`c90b63e` 已完成对应的本地构建/回归；`b1e2ca3` 之后在当前 NAS v1 运行边界下没有重新部署，后续前端拆分仅有 Mock Chromium 证据。该 smoke 不等价于登录后每个 Root 业务页面的完整 NAS 验收。代码层回滚点为 `d579004^`；本轮部署配置回滚点为 `b1e2ca3^`，部署层业务回滚点仍为保留的旧镜像 tag。

## 发布与后续边界

本轮功能、门禁与部署提交包括：`e265b9d`、`5ddb0e4`、`02ff4ba`、`404f8ed`、`73d5853`、`92fd3cd`、`358f978`、`1d4120c`、`f9adacc`、`5728b65`、`abaa27a`、`46a087b`、`05047f1`、`922407a`、`739d493`、`04ff791`、`97bb1ff`、`abccab6`、`cb78cd0`、`56cf1c8`、`99d0091`、`a256f87`、`d579004`、`699e925`、`5ab5bb5`、`dbf361c`、`9ef8b4e`、`69af511`、`6476632`、`b151fd5`、`c4c3b82`、`429e8c0`、`ecc158b`、`1d54eac`、`6b5d089`、`e1f39eb`、`b1e2ca3`、`ae288b9`、`e7c0037`、`fb99c56`、`6c3aaec`、`07f0a08`、`4ccb47e`、`fd9356e`、`374b6d3`、`10f341c`、`a286377`、`9db173d`、`230b32f`、`c79ab52`、`23c4e5a`、`dd1e290`、`a4691b8`、`c90b63e`；本次部署验证使用 `2026.8.24-v1`，镜像 digest 为 `sha256:b4bc26b5fee7cd4421058bf5d8552c3ef930fd4227a47eac5eb1e900fd125ca9`。Actions 最新 main push run `32684025341` 已实际 success；Release job 的 tag 路径只做了配置和一致性校验，尚未主动创建实际 release tag。

后续应优先清理 InfrastructureView 残留 CSS、核对 Docker HostPath/Network/Volume 能力、补 Database/Logs/Monitoring/Users/Settings 的更多危险操作与 loading/empty/error/dialog 真实浏览器验收，并在用户文档修改合并后同步 README/架构/发布文档；再处理 bundle、压缩、lint/format、基础镜像 pin 和实际 tag release smoke。静态缓存与日志轮转已在本轮完成，不再列为延期项。Users 仍保持既定 `minLength=6`，没有新增 RBAC/MFA 或安全加固任务。
